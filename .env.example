# Copy to .env and fill in the values. .env is gitignored.

# Google OAuth client secret for the Colab compute runtime (used by
# `lflow auth colab`). For the Desktop OAuth client this value is not
# confidential, but it is kept out of source control.
LFLOW_COLAB_OAUTH_SECRET=
